apiVersion: apps/v1
kind: Deployment
metadata:
  name: kitchen
  namespace: coffeeshop
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kitchen
  template:
    metadata:
      labels:
        app: kitchen
    spec:
      containers:
      - name: kitchen
        image: meraviglioso8/go-coffeeshop-kitchen:dev-latest
        env:
          - name: APP_NAME
            value: coffeeshop-kitchen
          - name: IN_DOCKER
            value: "true"
          - name: PG_URL
            valueFrom:
              secretKeyRef:
                name: db-credentials
                key: PG_URL
          - name: PG_DSN_URL
            valueFrom:
              secretKeyRef:
                name: db-credentials
                key: PG_DSN_URL
          - name: RABBITMQ_URL
            value: amqp://$(RABBITMQ_DEFAULT_USER):$(RABBITMQ_DEFAULT_PASS)@rabbitmq:5672/